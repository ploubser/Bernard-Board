<div class="ui-widget-header">
    <%= @title %>
</div>

<% if @parameters["remote"] == "true" %>
    <% image = "http://#{@parameters["url"]}/#{@parameters["image"]}" %>
<% else %>
    <% image = @parameters["image"] %>
<% end %>

<%= image_tag(image) %>

